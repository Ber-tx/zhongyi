# TCM AI 诊断系统 - 部分板块报告生成改造总结

## 🎯 改造目标
在每个诊断板块完成后，用户可以选择：
1. **继续下一个诊断板块** (返回诊断选择页)
2. **生成当前已完成板块的报告** (PDF/打印)

生成的报告格式与四诊完整报告相同，仅显示已完成的诊断板块。

---

## 📋 改造清单

### 1️⃣ 后端改造 (Java Spring Boot)

**文件: `demo/src/main/java/com/tx/demo/controller/ReportController.java`**

#### 修改内容：
- ✅ **ReportRequest 类** 新增字段：
  ```java
  public String completedTypes; // 逗号分隔的已完成诊断类型
  ```

- ✅ **generateReport() 方法** 改进：
  - 移除强制要求所有四诊完成的限制
  - 支持 `completedTypes` 参数传入
  - 只需至少1个诊断板块完成即可生成报告

- ✅ **isIncomplete() → hasCompletedDiagnosis()** 重构：
  - 原：要求至少2个诊断完成
  - 新：只要至少1个诊断完成就允许生成报告

- ✅ **buildDiagnosisInfo() 重载**：
  - 增加 `completedTypes` 参数
  - 支持向LLM传递哪些板块已完成的信息

### 2️⃣ Python后端改造 (FastAPI)

**文件: `tcm-ai-service/api/router_synthesis.py`**

#### 修改内容：
- ✅ **build_tcm_prompt()** 改进：
  - 新增 `completedTypes` 识别
  - 在部分板块情况下显示 "✓已完成" 和 "✗未完成" 标记
  - 根据完成度调整诊断指示语
  - 提醒用户补充完成其他诊断

- ✅ **generate_fallback_synthesis()** 改进：
  - 支持显示部分板块完成的回退方案
  - 添加"请完成全部诊断后重新生成"提示

### 3️⃣ 前端改造 (Vue 3)

#### Wang.vue (望诊) 修改：
- ✅ 完成后显示3个按钮：
  - 重新分析
  - 继续下一个诊断
  - **生成当前报告** ⭐
- ✅ 新增 `generatePartialReport()` 方法
- ✅ `completedTypes: 'wang'` 传递给后端

#### DetectSelect.vue (诊断选择页) 改进：
- ✅ **动态显示完成状态** (n/4 板块)：
  ```
  已完成 2/4 个诊断板块
  ✓ 望诊  ✓ 闻诊
  ```

- ✅ **动态报告按钮**：
  - 任何板块完成后即可生成报告
  - 显示 "生成报告 (n个板块)" 提示
  - 按钮文本自适应：
    - 1个: "生成报告 (1个板块)"
    - 4个: "生成四诊合参报告"

- ✅ **completedTypes 参数传递**：
  ```javascript
  completedTypes: ['wang', 'wen_audio', ...].join(',')
  ```

#### Report.vue (报告展示) 改进：
- ✅ **动态报告标题**：
  - 4/4: "四诊合参诊断报告"
  - 1/4: "单板块诊断报告 - 望诊"
  - 2-3/4: "部分诊断报告 (2/4板块)"

- ✅ **完成度徽章** (右上角)：
  - 未完整: "75% 完成度"（橙色）
  - 完整: "✓ 四诊完整"（绿色）

- ✅ **自动计算完成度**：
  ```javascript
  const calculateCompletion = () => {
    let completedCount = 0;
    if (diagnosis.wang) completedCount++;
    if (diagnosis.wen_audio) completedCount++;
    if (diagnosis.wen_questionnaire) completedCount++;
    if (diagnosis.qie) completedCount++;
    completionPercentage.value = (completedCount / 4) * 100;
  }
  ```

- ✅ **条件显示诊断内容**：
  - 已有数据则显示具体诊断结果
  - 无数据则显示 "暂未进行xxx检查" 提示
  - 综合建议自适应已完成的数据

### 4️⃣ 新增工具函数

**文件: `src/utils/reportUtils.js`**

提供可复用的报告生成工具：
```javascript
// 生成部分板块报告
generatePartialDiagnosisReport(diagnosisType, patientId, idCard)

// 生成多板块报告
generateMultiBlockReport(completedTypes, patientId, idCard)

// 获取已完成的诊断类型
getCompletedDiagnosisTypes()

// 获取诊断类型中文名
getDiagnosisTypeName(type)
```

---

## 🔄 完整工作流

### 用户体验流程：

```
1. 用户进入诊断选择页 (DetectSelect.vue)
   ↓
2. 点击任一诊断板块进入（如：望诊）
   ↓
3. 完成诊断采集 (Wang.vue)
   ↓
4. 显示完成界面，用户有两个选择：
   
   ✅ 选择 "继续下一个诊断"
   │  └→ 返回 DetectSelect.vue
   │     └→ 显示已完成状态 "已完成 1/4"
   │        └→ 用户可继续其他诊断或直接生成报告
   │
   ✅ 选择 "生成当前报告"
      └→ 跳转 Report.vue (同时传递 completedTypes='wang')
         └→ 后端生成仅包含望诊的报告
         └→ 显示 "单板块诊断报告 - 望诊"
         └→ 支持导出PDF/打印
         └→ 综合建议建议补完其他诊断
```

### 数据流向：

```
前端 Wang.vue
  └─ generatePartialReport('wang')
     └─> axios.post("/api/report/generate", {
           patientId: 123,
           idCard: "xxx",
           completedTypes: 'wang'  ⭐
         })
         └─> 后端 ReportController.generateReport()
             └─> buildDiagnosisInfo(patient, diagnosis, 'wang')
                 └─> 只包含 wang 的诊断数据
             └─> callLlmForSynthesis()
                 └─> Python LLM API
                     └─> build_tcm_prompt()
                         └─> 识别 completedTypes='wang'
                             └─> 生成支持部分板块的提示词
                             └─> 返回综合诊断建议

前端 Report.vue
  └─ 显示报告数据
     └─> calculateCompletion()
         └─> 检测哪些诊断有数据
             └─> 计算 completionPercentage
                 └─> 更新报告标题和徽章
```

---

## 🎨 UI 视觉改进

### 1. DetectSelect 页面：
```
┌─────────────────────────────────────┐
│     已完成 2/4 个诊断板块            │
│  ✓ 望诊    ✓ 闻诊                   │
│                                      │
│  [ 生成报告 (2个板块) ]              │
│  [ 继续完成其他诊断 ]                │
└─────────────────────────────────────┘
```

### 2. Report 页面头部：
```
┌───────────────────────────────────────┐
│ [←返回]  部分诊断报告 (2/4板块)  [75% 完成度]  │
│                                       │
│  [导出PDF] [打印]                    │
└───────────────────────────────────────┘
```

### 3. 诊断卡片（部分完成）：
```
望诊（舌象分析）✓
［已有数据］舌苔厚腻，舌质淡红...

闻诊（体质诊断）✓
［已有数据］湿热体质，置信度 95%

问诊（症状问卷）✗
暂未进行症状问卷调查

切诊（脉搏检测）✗
暂未进行脉搏检测
```

---

## ⚙️ 关键技术实现

### localStorage 用法：
```javascript
// 标记各板块完成
localStorage.setItem('wang_finished_id', patientId)
localStorage.setItem('wen_audio_finished_id', patientId)
localStorage.setItem('wen_questionnaire_finished_id', patientId)
localStorage.setItem('qie_finished_id', patientId)

// DetectSelect 读取状态
wangFinished.value = localStorage.getItem('wang_finished_id') === String(currentId)
```

### URL 参数传递：
```javascript
// DetectSelect → Report
router.push({
  path: '/report',
  query: { 
    id: patientId,
    completedTypes: 'wang,wen_audio'  // 关键！
  }
})

// Report 中接收
const completedTypesParam = route.query.completedTypes // 'wang,wen_audio'
```

### 后端条件判断：
```java
// 仅需至少1个诊断完成
if (!hasCompletedDiagnosis(diagnosis)) {
  return Result.error("请完成至少一个诊断板块");
}

// 提交已完成的类型到LLM
Map<String, Object> info = buildDiagnosisInfo(patient, diagnosis, completedTypes);
String synthesis = callLlmForSynthesis(info);
```

---

## 🧪 测试建议

1. **单板块生成**：完成望诊后直接生成报告
2. **多板块生成**：完成望+闻诊后生成报告
3. **完整生成**：完成所有板块后生成报告
4. **报告对比**：检查标题、完成度、综合建议是否正确
5. **导出功能**：验证PDF/打印中部分板块是否正确显示
6. **重新诊断**：已完成用户能否重新诊断并更新报告

---

## 📝 后续优化方向

1. ✏️ 支持编辑已完成的诊断（重新拍照/录音）
2. 📊 显示诊断完成时间戳
3. 🔒 添加诊断版本管理（修改后自动生成新报告）
4. 💾 支持部分诊断结果的本地草稿保存
5. 📧 支持邮件发送报告（PDF）

---

## 总结

✅ 架构已完全支持部分板块诊断报告生成
✅ 用户体验友好，清晰显示完成进度
✅ 报告格式统一，自适应已完成板块
✅ 后端接口扩展，兼容既有完整四诊流程
✅ LLM提示词智能适配，生成针对性建议
