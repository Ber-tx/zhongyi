spring:
  application:
    name: zhongyi-web-management
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/zhongyi?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true}
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: ${SPRING_DATASOURCE_USERNAME:root}
    password: ${SPRING_DATASOURCE_PASSWORD:2018620530tX@}
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.tx.demo.entity
  configuration:
    log-impl: org.apache.ibatis.logging.slf4j.Slf4jImpl
    map-underscore-to-camel-case: true
    lazy-loading-enabled: false
    cache-enabled: false

logging:
  level:
    com.tx.demo.mapper: debug

jwt:
  secret: "ZhongYiAdminSecretKey2024@#$!xTx"
  expiration: 28800000