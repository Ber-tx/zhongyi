<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tx.demo.mapper.PatientMapper">

    <select id="findByIdCard" resultType="com.tx.demo.entity.Patient">
        SELECT id, name, gender,  birthday, address, id_card as idCard FROM patients WHERE id_card = #{idCard}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO patients (name, gender,  birthday, address, id_card) VALUES (#{name}, #{gender},  #{birthday}, #{address}, #{idCard})
    </insert>

    <update id="update">
        UPDATE patients
        SET name = #{name},
        gender = #{gender},
        birthday = #{birthday},
        address = #{address}
        WHERE id_card = #{idCard}
    </update>

</mapper>